#include <iostream>
#include <cstring>
#include <vector>
#include <typeinfo>

using namespace std;
using std::vector;

int main()
{
	/*vector<int> v = {2,4,6};
	auto pbeg = v.begin();
	while(pbeg!=v.end()&&*pbeg>=0)
		cout<<*++pbeg<<endl;*/
	int* ptr;
	vector<int> vec;
	int ival;
	if(ptr!=0&&*ptr++)
		cout<<"d"<<endl;
}					