#include <map>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

int main()
{
	map<string,vector<int>> fy_map;
	map<string,vector<int>>::iterator it = fy_map.find("handsome");
}