#include <iostream>
#include <cstring>
#include <vector>
#include <typeinfo>

using namespace std;
using std::vector;

class matrix{};

bool compare(matrix &ma1,matrix &ma2);

vector<int> change_val(int i,vector<int> vi);
	
int count(char c){
}

int main()
{
	count('a');
}
